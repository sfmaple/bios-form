# bios-form

## FieldSchema

```typescript
type FieldSchema = {
  indexes;
  id;
  name;
  depend;
  title;
  widget;
  rules: {
    check;
    enter;
  };
  common;
  // base on id
  action;
  props
}
```
